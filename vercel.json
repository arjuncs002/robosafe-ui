{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

6. Press **Ctrl+S** to save

---

## **STEP 5: Check/Update `.gitignore` file**

1. Look in the sidebar for a file named `.gitignore`
   - **If it EXISTS**: Click on it to open
   - **If it DOESN'T exist**: Create it (Right-click → New File → `.gitignore`)

2. Make sure it has these lines (copy-paste all of this):
```
node_modules/
dist/
build/
.env
.env.local
.env.production
```

3. Press **Ctrl+S** to save

---

## ✅ **FRONTEND CHECKLIST**

Your sidebar should now show:
- ✅ `.env` (new file you created)
- ✅ `.env.local` (new file you created)
- ✅ `vercel.json` (new file you created)
- ✅ `.gitignore` (updated)
- `src/` (already existed)
- `package.json` (already existed)

**Screenshot what you should see:**
```
📁 robosafe-frontend
  📄 .env              ← NEW
  📄 .env.local        ← NEW
  📄 .gitignore        ← UPDATED
  📄 vercel.json       ← NEW
  📁 src
  📄 package.json
  📄 vite.config.js
```

---

# 🔷 PART 2: BACKEND FILES

## **STEP 6: Open Backend Folder in VS Code**

1. Click **File** → **Open Folder** (or **File** → **New Window** if you want both open)
2. Navigate to your **robosafe-backend** folder (where `main.py` is)
3. Click **Select Folder**

---

## **STEP 7: Create `.gitignore` file**

1. Look at the left sidebar, you should see:
   - `main.py`
   - `db.py`
   - Maybe `robosafe.db`

2. **Right-click on EMPTY SPACE** in the sidebar
3. Click **"New File"**
4. Type: `.gitignore`
5. Press **Enter**
6. Paste this EXACTLY:
```
__pycache__/
*.pyc
*.db
*.pt
venv/
env/
.env
robosafe.db
best.pt
runs/